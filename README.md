# WurmUnlimited-Chirp-Info
Simple server mod to display time to next farm tick (chirp) on login and by command.

Commands /chirp, /nextchirp, or /nextcrop will display the current time to next farm tick.

